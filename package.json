{
  "name": "passport-reader",
  "version": "0.1.0",
  "description": "Reference decoder, verification vectors, and a minimal developer viewer for Pilier digital product passports.",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check-vectors": "node scripts/check-vectors.js",
    "view": "node scripts/view.js"
  },
  "dependencies": {
    "@polkadot/api": "^16.5.6",
    "@polkadot/util-crypto": "^14.0.3"
  }
}
